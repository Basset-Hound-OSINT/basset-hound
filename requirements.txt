# Flask (DEPRECATED - migration to FastAPI complete)
# Flask==3.1.0
# The Flask files (app.py, profiles.py, reports.py) are deprecated.
# All routes have been migrated to FastAPI in api/routers/frontend*.py
# Flask can be safely removed from the project.

# Core FastAPI (handles both API and frontend templates)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
starlette>=0.41.0
jinja2>=3.1.0  # Template rendering for frontend

# Configuration
python-dotenv==1.1.0
PyYAML==6.0.2
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0
email-validator>=2.1.0

# Database
neo4j==5.28.1

# Task Queue
celery[redis]>=5.3.0
redis>=5.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.12

# MCP Server
mcp>=1.0.0

# File handling
aiofiles>=24.1.0
python-magic>=0.4.27

# Report generation
markdown>=3.0.0
# weasyprint>=60.0  # Optional: for PDF generation (requires system dependencies)

# HTTP client (for MCP and external APIs)
httpx>=0.27.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.27.0

# Development
ruff>=0.8.0
mypy>=1.13.0
rapidfuzz>=3.0.0

# Phone number parsing and formatting
phonenumbers>=8.13.0
